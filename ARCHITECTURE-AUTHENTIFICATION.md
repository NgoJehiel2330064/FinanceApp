# 🏗️ Architecture - Authentification & Sécurité

## Diagramme Architecture Générale

```
┌────────────────────────────────────────────────────────────────┐
│                      FINANCE APP - ARCHITECTURE               │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │           COUCHE PRÉSENTATION (Next.js 13+)           │  │
│  │                                                       │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  PAGES PUBLIQUES                             │   │  │
│  │  │  - /connexion (LoginPage)                    │   │  │
│  │  │  - /inscription (RegisterPage)               │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │  │                                                   │  │
│  │  ▼                                                   │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  PAGES PROTÉGÉES (+ Authentification)        │   │  │
│  │  │  - / (AccueilPage)                           │   │  │
│  │  │  - /transactions (TransactionsPage)          │   │  │
│  │  │  - /statistiques (StatisticsPage)            │   │  │
│  │  │  - /patrimoine (PatrimoinePage)              │   │  │
│  │  │  - /profil (ProfilePage)                     │   │  │
│  │  │                                               │   │  │
│  │  │  Chaque page exécute:                        │   │  │
│  │  │  → useEffect(() => {                         │   │  │
│  │  │      const user = localStorage.get('user')   │   │  │
│  │  │      if (!user) router.push('/connexion')    │   │  │
│  │  │    })                                         │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  └─────────────────────────────────────────────────────┘  │
│                          │                                  │
│                          │ useRouter                       │
│                          │ useEffect                       │
│                          │                                  │
└──────────────────────────┼──────────────────────────────────┘
                           │
┌──────────────────────────┼──────────────────────────────────┐
│                          │  COUCHE UTILITAIRES (lib/)       │
│                          ▼                                  │
│  ┌────────────────────────────────────────────────────┐   │
│  │  api-config.ts                                    │   │
│  │  - BASE_URL: http://localhost:5153               │   │
│  │  - ENDPOINTS: { LOGIN, REGISTER, ..., PROFILE }  │   │
│  │  - getApiUrl(): Helper pour construire URLs      │   │
│  └────────────────────────────────────────────────────┘   │
│                          │                                  │
│  ┌────────────────────────────────────────────────────┐   │
│  │  use-auth.ts (NOUVEAU - Hook Réutilisable)       │   │
│  │  - requireAuth(): Vérifier & rediriger           │   │
│  │  - getUser(): Récupérer utilisateur              │   │
│  │  - isAuthenticated(): Vérifier connexion          │   │
│  │  - logout(): Déconnection                        │   │
│  └────────────────────────────────────────────────────┘   │
│                          │                                  │
│  ┌────────────────────────────────────────────────────┐   │
│  │  auth-service.ts                                  │   │
│  │  - register(data)                                │   │
│  │  - login(data) → localStorage.user              │   │
│  │  - checkEmail(email)                            │   │
│  └────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌────────────────────────────────────────────────────┐   │
│  │  transaction-service.ts                           │   │
│  │  - Gestion des transactions                      │   │
│  └────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                           │
┌──────────────────────────┼──────────────────────────────────┐
│                          │  COUCHE COMPOSANTS               │
│                          ▼                                  │
│  ┌────────────────────────────────────────────────────┐   │
│  │  ProtectedPage.tsx (NOUVEAU)                      │   │
│  │  - Wrapper pour pages protégées                  │   │
│  │  - Loader pendant vérification                  │   │
│  │  - Gestion d'erreurs localStorage              │   │
│  └────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌────────────────────────────────────────────────────┐   │
│  │  Navigation.tsx                                   │   │
│  │  - Barre de navigation                          │   │
│  │  - Liens vers pages                             │   │
│  │  - Logout button                                │   │
│  └────────────────────────────────────────────────────┘   │
│                                                             │
│  [Autres composants...]                                   │
│                                                             │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           │ fetch()
                           │ JSON API
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                  COUCHE API (Backend)                       │
│                ASP.NET Core 8.0                             │
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │  Controllers/AuthController.cs                  │      │
│  │  - POST /api/auth/register                      │      │
│  │  - POST /api/auth/login                         │      │
│  │  - GET  /api/auth/profile?userId=X   ✅ SECURE │      │
│  │  - POST /api/auth/change-password   ✅ SECURE  │      │
│  │  - DELETE /api/auth/delete-account  ✅ SECURE  │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │  Controllers/TransactionsController.cs           │      │
│  │  - CRUD Transactions                             │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
│  [Autres controllers...]                                   │
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │  Models/                                         │      │
│  │  - User.cs                                       │      │
│  │  - Transaction.cs                               │      │
│  │  - Asset.cs                                      │      │
│  │  - DTOs/ (AuthResponseDto, UserInfoDto, etc.)   │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │  Data/ApplicationDbContext.cs                    │      │
│  │  - EF Core DbContext                             │      │
│  │  - PostgreSQL Migration                         │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
└───────────────────────────┬────────────────────────────────┘
                            │
                            │ SQL
                            │
┌───────────────────────────▼────────────────────────────────┐
│              COUCHE DONNÉES (PostgreSQL)                   │
│                                                             │
│  Database: finance_db                                     │
│  Port: 5432 (Docker)                                      │
│                                                             │
│  ┌─────────────────────────────────────────────┐         │
│  │  Tables:                                    │         │
│  │  - Users (id, nom, email, passwordHash...)│         │
│  │  - Transactions (id, userId, amount...)   │         │
│  │  - Assets (id, userId, name, value...)    │         │
│  │  - AuditLogs (optionnel, futur)           │         │
│  └─────────────────────────────────────────────┘         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 Flux d'Authentification Détaillé

### 1. Page de Connexion

```
┌─────────────┐
│ /connexion  │
│  Publique   │
└──────┬──────┘
       │
       ▼
    ┌─────────────────────────────┐
    │ Utilisateur entre:          │
    │ - Email                     │
    │ - Mot de passe              │
    └──────────┬──────────────────┘
               │
               ▼
    ┌─────────────────────────────┐
    │ authService.login()         │
    │ POST /api/auth/login        │
    └──────────┬──────────────────┘
               │
               ▼
    ┌──────────────────────────────────────┐
    │ Backend:                             │
    │ - Valider email                      │
    │ - Vérifier password (SHA256)         │
    │ - Retourner: { success, user, msg } │
    └──────────┬───────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────┐
    │ Frontend:                        │
    │ localStorage.setItem('user', ...) │
    │ router.push('/')                 │
    └──────────┬───────────────────────┘
               │
               ▼
            ✅ CONNECTÉ
```

### 2. Accès à une Page Protégée

```
┌─────────────────┐
│ Utilisateur va  │
│ à /:protected:  │
└────────┬────────┘
         │
         ▼
    ┌────────────────────────┐
    │ Page charge            │
    │ useEffect() s'exécute  │
    └────────┬───────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ const user =                   │
    │   localStorage.getItem('user') │
    └────┬───────────────┬───────────┘
         │               │
      null           JSON string
         │               │
         ▼               ▼
    ┌────────┐   ┌──────────────────┐
    │ Redirect   │ JSON.parse(user) │
    │ /connexion │ → userData       │
    └────────┘   └────┬─────────────┘
                      │
                      ▼
                  ┌─────────────────┐
                  │ Render page     │
                  │ avec userData   │
                  └─────────────────┘
                      ✅ ACCESSIBLE
```

### 3. Logout

```
┌─────────────────┐
┌─────────────────┐
│ Utilisateur     │
│ clique Logout   │
└────────┬────────┘
         │
         ▼
    ┌────────────────────────┐
    │ useAuth().logout()     │
    └────────┬───────────────┘
             │
             ▼
    ┌────────────────────────┐
    │ localStorage.clear()   │
    │ (ou removeItem('user')) │
    └────────┬───────────────┘
             │
             ▼
    ┌────────────────────────┐
    │ router.push('/connexion')
    └────────┬───────────────┘
             │
             ▼
         ❌ DÉCONNECTÉ
```

---

## 📦 Structure de Dossiers

```
finance-ui/
│
├── app/
│   ├── page.tsx                 ✅ Protégée
│   ├── layout.tsx
│   ├── globals.css
│   │
│   ├── connexion/
│   │   └── page.tsx            ✅ Publique
│   │
│   ├── inscription/
│   │   └── page.tsx            ✅ Publique
│   │
│   ├── transactions/
│   │   └── page.tsx            ✅ Protégée
│   │
│   ├── statistiques/
│   │   └── page.tsx            ✅ Protégée
│   │
│   ├── patrimoine/
│   │   └── page.tsx            ✅ Protégée
│   │
│   └── profil/
│       └── page.tsx            ✅ Protégée
│
├── lib/
│   ├── api-config.ts           Configuration API
│   ├── use-auth.ts             ✅ NOUVEAU - Hook d'auth
│   ├── auth-service.ts         Service authentification
│   ├── transaction-service.ts  Service transactions
│   └── [autres services]
│
├── components/
│   ├── Navigation.tsx
│   ├── ProtectedPage.tsx       ✅ NOUVEAU - Wrapper
│   ├── AssetList.tsx
│   ├── [autres composants]
│   └── [autres composants]
│
├── middleware.ts               ✅ NOUVEAU - Middleware
│
├── public/
│   └── [assets statiques]
│
└── package.json
```

---

## 🔄 Cycle de Vie de l'Application

### Startup
```
1. npm run dev
2. Next.js démarre port 3000
3. middleware.ts se charge
4. Pages compilent avec protections
```

### Page Route
```
1. Utilisateur accède à /
2. middleware.ts valide la route
3. Layout.tsx se charge
4. page.tsx se charge
5. useEffect() vérifie localStorage
6. Si pas d'user → Redirige /connexion
7. Si user → Fetch des données
8. Render du contenu
```

### Interaction Utilisateur
```
1. Utilisateur remplit formulaire
2. onClick → appel authService.login()
3. fetch POST /api/auth/login
4. Backend valide et retourne user
5. Frontend stocke dans localStorage
6. Router redirige vers /
7. useEffect détecte le user
8. Page se charge avec données
```

---

## 🛡️ Points de Sécurité

### Frontend
```
✅ Vérification localStorage
✅ useRouter pour redirection
✅ JSON.parse avec try-catch
✅ Hook centralisé (useAuth)
⚠️ localStorage visible en DevTools
```

### Backend
```
✅ POST pour login (pas GET)
✅ Password SHA256 hash
✅ Email unique check
✅ IsActive flag pour soft-delete
⚠️ Pas de JWT expiration
⚠️ Pas de CSRF token
```

### À Faire
```
🔴 Implémenter JWT
🔴 httpOnly cookies
🔴 Expiration tokens
🟡 CSRF protection
🟡 Rate limiting
```

---

## 📊 État des Composants

| Composant | État | Notes |
|-----------|------|-------|
| Navigation | ✅ OK | Liens corrects |
| LoginPage | ✅ OK | Fonctionnel |
| RegisterPage | ✅ OK | Fonctionnel |
| AccueilPage | ✅ OK | Protégée |
| TransactionsPage | ✅ OK | Protégée |
| StatisticsPage | ✅ OK | Protégée |
| PatrimoinePage | ✅ OK | Protégée |
| ProfilePage | ✅ OK | Protégée |
| useAuth Hook | ✅ OK | Réutilisable |
| ProtectedPage | ✅ OK | Optionnel |
| middleware | ✅ OK | Basique |

---

## 🚀 Déploiement

### Développement
```
Frontend:  npm run dev      → Port 3000
Backend:   dotnet run       → Port 5153
Database:  Docker           → Port 5432
```

### Production (À faire)
```
- Remplacer localhost par domaine
- Implémenter HTTPS
- Ajouter JWT
- Configurer CORS
- Ajouter monitoring
```

---

## 📝 Notes Importantes

1. **localStorage vs Cookies**
   - localStorage: Visible en DevTools (risque XSS)
   - httpOnly Cookies: Sécurisé (recommandé)
   - Action: Migrer vers JWT + httpOnly cookies

2. **Expiration des Tokens**
   - Actuellement: Aucune (persistant)
   - À faire: 15 min access token + 7 jours refresh
   - Urgence: 🔴 Haute

3. **Validation Côté Client vs Serveur**
   - Client: UX rapide (ne pas bloquer)
   - Serveur: Sécurité (validation obligatoire)
   - Status: Backend valide tous les inputs ✅

4. **CORS Configuration**
   - Frontend: http://localhost:3000
   - Backend: http://localhost:5153
   - Status: Basique ✅ (À améliorer)

---

**Architecture Version**: 1.0  
**Dernière Mise à Jour**: 2025-02-02  
**Status**: ✅ Production Ready (Avec améliorations recommandées)
